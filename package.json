{
  "name": "vault-hunters-map-tracker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install": " cd client && npm i && cd ../server && npm i",
    "build": "cd client && npm run build && cd ../server && npm run build",
    "start": "node server/dist/index.js"
  }
}
